let a; //any by default
let b:number;
let c:boolean;
let d:string;
let e:any;//like js let, type not known yet
let f: number[]; //array
let g:Object;// object

enum color{Red=0,Green=1,Blue=2}; //enums in JS
let backgroundColor = color.Red; 